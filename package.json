{
  "name": "small-business-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Small Business Customer Relationship Management (CRM) Platform",
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix backend run build:prisma && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node backend/src/server.js"
  }
}
